import java.util.*;
public class WithdrawMenu{
    public static void menu(){
        Scanner sc = new Scanner(System.in);
        int choice;
        //Print list of choice of registered camps
        System.out.println("Select camp to withdraw from: ");
        choice = sc.nextInt();
        //Add slot
        //Registered camp can have attribute to see if still registered or not
    }
}
